data:
  processed_path: "data/processed/wine_processed.csv"

training:
  model: "RandomForestRegressor"
  n_estimators: 200
  max_depth: 12
  random_state: 42
  n_jobs: -1

mlflow:
  tracking_uri: "file:///D:/ML_Services/ml_ETL_project/mlruns"
  experiment_name: "wine_quality"

paths:
  models: "models"
  reports: "reports"
  artifacts: "artifacts"
